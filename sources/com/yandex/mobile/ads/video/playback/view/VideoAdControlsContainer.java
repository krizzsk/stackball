package com.yandex.mobile.ads.video.playback.view;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.FrameLayout;

public class VideoAdControlsContainer extends FrameLayout {
    public VideoAdControlsContainer(Context context) {
        super(context);
    }

    public VideoAdControlsContainer(Context context, AttributeSet attributeSet) {
        super(context, attributeSet);
    }

    public VideoAdControlsContainer(Context context, AttributeSet attributeSet, int i) {
        super(context, attributeSet, i);
    }
}
